-- generated by cds-compiler version 2.11.4
VIEW anubhav_db_CDSView_ProductViewSub AS SELECT
  prod_0.PRODUCT_ID AS ProductId,
  texts_2.DESCRIPTION AS Description,
  (SELECT
      round(SUM(a_1.GROSS_AMOUNT), 2) AS "SUM"
    FROM anubhav_db_transaction_poitems AS a_1
    WHERE a_1.PRODUCT_GUID_NODE_KEY = prod_0.NODE_KEY) AS PO_SUM
FROM (anubhav_db_master_product AS prod_0 LEFT JOIN anubhav_db_master_product_texts AS texts_2 ON texts_2.NODE_KEY = prod_0.NODE_KEY)